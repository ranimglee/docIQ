import {useRef,useState} from 'react'; import {Upload} from 'lucide-react';
export function UploadBox({onFile,busy}:{onFile:(f:File)=>void;busy:boolean}) { const ref=useRef<HTMLInputElement>(null); const [error,setError]=useState(''); const choose=(f?:File)=>{if(!f)return; if(!['application/pdf','image/png','image/jpeg'].includes(f.type)){setError('Choose a PDF, PNG, or JPG.');return}setError('');onFile(f)}; return <div className="upload" onDragOver={e=>e.preventDefault()} onDrop={e=>{e.preventDefault();choose(e.dataTransfer.files[0])}} onClick={()=>!busy&&ref.current?.click()}><Upload size={28}/><strong>{busy?'Processing document…':'Drop a document here or click to browse'}</strong><span>PDF, PNG, JPG · up to 20 MB</span>{error&&<small className="error">{error}</small>}<input ref={ref} type="file" accept=".pdf,.png,.jpg,.jpeg" hidden onChange={e=>choose(e.target.files?.[0])}/></div> }
